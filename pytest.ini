[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py
addopts = -ra --strict-markers --tb=short
markers =
    slow: long-running tests
    e2e: playwright end-to-end (not run by default)
    security: OWASP-aligned tests
